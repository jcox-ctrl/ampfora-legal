# ampfora-legal

Public pages for **Ampfora** (Reach Mobile, LLC), served by GitHub Pages.

Do not hand-edit `privacy.html` or `terms.html`. They are generated from
`legal/*.md` in the app repo via `npm run build:legal`.
